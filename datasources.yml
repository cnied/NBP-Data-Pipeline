apiVersion: 1

datasources:
  - name: Postgres
    type: postgres
    url: postgres:5432
    user: ${POSTGRES_USER}
    secureJsonData:
      password: ${POSTGRES_PASSWORD}
    jsonData:
      sslmode: 'disable'
      database: ${POSTGRES_DB}
